import React from 'react';
import Animes from './Animes';

const MainContainer = () => {
   return (
      <div>
         <Animes />
         <h1>Thanks for voting!</h1>
      </div>
   );
};

export default MainContainer;
